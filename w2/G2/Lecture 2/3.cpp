#include <iostream>

using namespace std;

int main(){
  // (==, !=, >, <, >=, <=)

  cout << (2 == 2) << endl; // 1 == 2?
  cout << (2.2 != 2.4) << endl; // 2.2 != 2.4?
  cout << ('A' <= 'B') << endl; // 65 == 66?

  return 0;
}